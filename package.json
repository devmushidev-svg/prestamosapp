{
  "name": "multiprestamos",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev -w apps/web",
    "build": "npm run build -w apps/web",
    "preview": "npm run preview -w apps/web",
    "demo:seed": "node scripts/seed-demo.mjs --confirm-demo",
    "demo:clean": "node scripts/seed-demo.mjs --confirm-demo --clean"
  }
}
